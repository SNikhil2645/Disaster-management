{
  "buildCommand": "cd client && npm run build && cd ../server && npm run build",
  "outputDirectory": "server/dist",
  "installCommand": "cd client && npm install && cd ../server && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ],
  "functions": {
    "api/**/*.js": {
      "memory": 256,
      "maxDuration": 10
    }
  }
}
